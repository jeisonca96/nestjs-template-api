<html>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width' />
    <title>System Alert: {{title}}</title>
    <style>
      /* Maintain the same CSS styles */
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        font-family: Arial, sans-serif;
      }
      .header {
        color: #2d3748;
        font-size: 24px;
        margin-bottom: 30px;
      }
      .content {
        background-color: #f7fafc;
        padding: 30px;
        border-radius: 8px;
      }
      .button {
        background-color: #4299e1;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 4px;
        display: inline-block;
      }
      .footer {
        margin-top: 30px;
        text-align: center;
        color: #718096;
        font-size: 12px;
      }
      .text-link {
        color: #4299e1;
        word-break: break-all;
      }
      .warning {
        color: #e53e3e;
        font-weight: bold;
      }
      .alert-details {
        margin: 20px 0;
      }
      .alert-meta {
        color: #718096;
        font-size: 14px;
        margin-top: 20px;
        padding-top: 10px;
        border-top: 1px solid #e2e8f0;
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>{{title}}</div>

      <div class='content'>
        <p>{{description}}</p>

        {{#if additionalInfo}}
          <div class='alert-details'>
            <p><strong>Additional Information:</strong></p>
            <p>{{additionalInfo}}</p>
          </div>
        {{/if}}

        {{#if actionLink}}
          <div style='margin: 30px 0;'>
            <a href='{{actionLink}}' class='button'>{{actionLinkText}}</a>
          </div>
        {{/if}}

        <div class='alert-meta'>
          <p>Date: {{timestamp}}</p>
          <p>Environment: {{environment}}</p>
          {{#if traceId}}
            <p>Trace ID: {{traceId}}</p>
          {{/if}}
        </div>
      </div>

      <div class='footer'>
        ©
        {{currentYear}}
        Doctors API. All rights reserved.<br />
        <p>This is an automated system message, please do not reply to this
          email.</p>
      </div>
    </div>
  </body>
</html>